resources:
  pipelines:
    pipeline_demo_pipeline:
      name: Demo_pipeline
      libraries:
        - glob:
            include: ../../src/Demo_pipeline/transformations/**
      schema: default
      continuous: false
      development: false
      photon: true
      channel: CURRENT
      catalog: workspace
      serverless: true
      root_path: ../../src/Demo_pipeline
